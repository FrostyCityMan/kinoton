spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver
  flyway:
    locations:
      - classpath:db/migration

app:
  storage:
    root: ${STORAGE_ROOT:/home/ubuntu/app/storage}
